const { downloadThreadImages } = require('./util/download-thread-images')
const { getBoardCatalog } = require('./util/get-board-catalog')
const { getThread } = require('./util/get-thread')

module.exports = {
  downloadThreadImages,
  getBoardCatalog,
  getThread
}
